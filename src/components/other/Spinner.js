/*
 * @flow
 */

import styled from 'styled-components';
import { Spinner as LUKSpinner } from 'lattice-ui-kit';

const Spinner = styled(LUKSpinner).attrs({ size: '2x' })`
  justify-self: center;
`;

export default Spinner;
