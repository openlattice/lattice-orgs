/*
 * @flow
 */

import { Models } from 'lattice';

const { FQN } = Models;

const FQNS = {
  OL_DATE_TIME: FQN.of('ol.datetime'),
  OL_ID: FQN.of('ol.id'),
  OL_STANDARDIZED: FQN.of('ol.standardized'),
};

export {
  FQNS,
};
